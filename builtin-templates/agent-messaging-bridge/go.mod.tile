module messaging-bridge

go 1.24

require github.com/xbin-dev/xbin/sdk v0.0.0

// The sdk is resolved by the workspace go.work (generated by xbind). Add your
// platform's client libraries here (they are fetched on the first build).
