#!/bin/sh
# Installed by `make hooks` (git config core.hooksPath .githooks). The
# sub-second subset of `make check`; the rest runs in CI and before a release.
set -e
cd "$(git rev-parse --show-toplevel)"
make -s fmt-check js-check
