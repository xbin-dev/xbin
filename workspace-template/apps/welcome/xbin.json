{
  // The welcome tile (static). Its "read the docs ↗" links open in a new
  // tab, which a sandboxed frame may only do with cap:open-links (ND11) —
  // the template workspace pre-approves it (see xbin.json at the root).
  "uses": [
    { "target": "cap:open-links", "role": "writer" }
  ]
}
