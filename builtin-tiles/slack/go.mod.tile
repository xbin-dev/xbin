module slack

go 1.24

require (
	github.com/gorilla/websocket v1.5.3
	github.com/xbin-dev/xbin/sdk v0.0.0
)

// The sdk is resolved by the workspace go.work (generated by xbind);
// gorilla/websocket (the Socket Mode connection) is fetched on first build.
