// tile-prometheus-viewer — builtin-tiles/prometheus-viewer/native.js as
// shipped (plans/native.md §18.7; it imports the page's prom.js), imported
// rather than copied so the fixture cannot drift from it.
import '../../../builtin-tiles/prometheus-viewer/native.js';
