// tile-egress-approver — builtin-tiles/egress-approver/native.js as shipped
// (plans/native.md §18.3; it imports the page's fmt.js), imported rather than
// copied so the fixture cannot drift from it.
import '../../../builtin-tiles/egress-approver/native.js';
