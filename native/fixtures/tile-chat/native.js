// tile-chat — builtin-tiles/chat/native.js as shipped (plans/native.md §18.8;
// its engine is the page's chat-core.js), imported rather than copied so the
// fixture cannot drift from it.
import '../../../builtin-tiles/chat/native.js';
